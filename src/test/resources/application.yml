spring:
  datasource:
    username: ${MODULITH_USERNAME}
    password: ${MODULITH_PASSWORD}
    url: jdbc:postgresql://localhost:5432/modulith

  docker:
    compose:
      skip:
        in-tests: false
logging:
  level:
    org.springframework.modulith: DEBUG